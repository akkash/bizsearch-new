# Dependencies
node_modules

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Build output
dist

# Supabase CLI temp
supabase/.temp

# Editor / OS
.DS_Store
*.log
.vscode/
.idea/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
e2e/.generated-ids.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Superdesign temp templates (init + design-system stay tracked)
.superdesign/tmp/
